# 实习简报（2026-03-12）

## 1. 本次工作结论

当前基线 commit id：

- [7b2e4599f8793ab74cafdb2ed8e45fcc12b5b006](https://github.com/HansBug/URSA-MATH/commit/7b2e4599f8793ab74cafdb2ed8e45fcc12b5b006)

我完整阅读了仓库内的 [paper.md](https://github.com/HansBug/URSA-MATH/blob/main/paper.md) 和 [paper_zh.md](https://github.com/HansBug/URSA-MATH/blob/main/paper_zh.md)，重点对齐了强化学习阶段（Stage III）里和 `URSA-RM-8B` 推理直接相关的公式与复现需求：

- 公式 5：`drop-moment` 检测
- 公式 6：PS-GRPO 奖励建模
- 公式 7：vanilla GRPO 组内优势
- 公式 9：两种 PRM-integrated GRPO 变体
- 公式 10：BoN 选择

基于这些要求，我把原本只输出 `min_score` / `avg_score` 的 `rm_8b` 示例，扩展成可以直接产出论文第 4 阶段所需关键中间量的结构化推理脚本，并同步补全了 standalone 版本与运行文档。

## 2. 相对 upstream `origin/main` 的差异

对比基线：

- `git fetch origin` 已执行
- `git log origin/main..HEAD` 为空
- 因此本次差异全部来自当前工作区未提交修改

当前新增/修改内容如下：

- [examples/run_ursa_rm_8b_score_example.py](https://github.com/HansBug/URSA-MATH/blob/main/examples/run_ursa_rm_8b_score_example.py)
  - 将单条 response 打分扩展为多 rollout 评估
  - 新增 `--responses-file`、重复 `--response`、`--ground-truth-answer`、`--outcome-reward`、`--rho`、`--gamma`
  - 新增逐步 `process_reward_sequence`
  - 新增 BoN、drop-moment、PS-GRPO、vanilla GRPO、Variant 1、Variant 2 的计算与 JSON 输出
  - 修正最终答案规范化逻辑，避免 `†Answer: 3` 被错误匹配成 `Answer: 3`
- [examples/run_ursa_rm_8b_score_example_standalone.py](https://github.com/HansBug/URSA-MATH/blob/main/examples/run_ursa_rm_8b_score_example_standalone.py)
  - 与普通版保持同一输出 schema
  - 保留 standalone 约束，不依赖仓库原始 `inference/` 与 `models/`
- [RUN_GUIDE.md](https://github.com/HansBug/URSA-MATH/blob/main/RUN_GUIDE.md)
  - 重写 `URSA-RM-8B` 使用说明，按论文公式解释输出字段
  - 补充多 rollout / `responses-file` 用法
  - 替换为本机真实跑通后的参考输出
- [report-2026-03-12.md](https://github.com/HansBug/URSA-MATH/blob/main/report-2026-03-12.md)
  - 记录本次阅读、实现、验证与差异总结

## 3. 论文公式到脚本输出的对应关系

- 公式 5 `delta_p^i`
  - 输出为 `drop_moment.relative_drops`、`drop_moment.max_relative_drop`、`drop_moment.triggered`
- 公式 6 `R^i`
  - 输出为 `paper_metrics.ps_grpo_reward`
- 公式 7 `A^i`
  - 输出为 `paper_metrics.vanilla_grpo_advantage`
- 公式 9 Variant 1
  - 输出为 `paper_metrics.variant1_rollout_reward` 与 `paper_metrics.variant1_advantage`
- 公式 9 Variant 2
  - 输出为 `paper_metrics.variant2_step_rewards`、`paper_metrics.variant2_process_advantage_term`、`paper_metrics.variant2_outcome_advantage_term`、`paper_metrics.variant2_step_advantages`
- 公式 10 BoN
  - 输出为 `bon_by_mean_process_reward`

默认示例还补上了论文里使用的超参数：

- `rho = 0.3`
- `gamma = 0.5`

## 4. 本机验证结果

已完成静态验证：

- `python -m py_compile examples/run_ursa_rm_8b_score_example.py`
- `python -m py_compile examples/run_ursa_rm_8b_score_example_standalone.py`

已完成 GPU 实测：

- `CUDA_VISIBLE_DEVICES=0 python examples/run_ursa_rm_8b_score_example.py --device cuda:0`
  - 成功输出 4 条 rollout 的 `process_reward_sequence`
  - BoN 选择结果：`selected_rollout_id=0`
  - `selected_avg_process_reward=0.954427`
  - `outcome_reward_mean=0.75`
  - `ps_grpo_reward_mean=0.625`
- `CUDA_VISIBLE_DEVICES=1 python examples/run_ursa_rm_8b_score_example_standalone.py --device cuda:0`
  - 输出 schema 与普通版一致
  - 核心数值与普通版一致
- `CUDA_VISIBLE_DEVICES=2 python examples/run_ursa_8b_torch_example.py --device cuda:0`
  - 成功加载并生成
- `CUDA_VISIBLE_DEVICES=3 python examples/run_ursa_8b_torch_example_standalone.py --device cuda:0`
  - 成功加载并生成

## 5. 观察与说明

- `URSA-RM-8B` 相关脚本已经足够覆盖论文强化学习阶段复现所需的 PRM 输出、中间奖励和组内优势字段。
- `URSA-8B` 生成示例虽然已经在本机跑通，但它在 `framework.png` 上仍生成 `†Answer: 4`。这属于基础生成模型行为，不是本次 `rm_8b` / RL 复现扩展的修改目标，因此本次未对生成脚本做对齐修正。
- 如果后续任务的数据答案匹配比 `ground_truth_answer` 更复杂，建议显式传 `--outcome-reward`，而不是依赖当前示例的字符串归一化比较。
